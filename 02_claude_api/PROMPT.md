Change the project to fullfill the following requirements:

Send the same request, but:
👉 add an explicit description of the response format
👉 add a limit on the response length
👉 add a condition for terminating the response (a stop sequence or an explicit instruction)

Compare the responses:
👉 without constraints
👉 with constraints

Result:
The same request with varying levels of control over the API response.

Implementation hints:
1. Remove the table, remove different system prompts, keep only the toxic one but make it a staff-level full-stack dev
2. Send multiple requests using params different configurations, display the request params next to the responses in a nice, human-readable way
