# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Only needed for identity-linked API keys, which must name the workspace they
# act in. If requests fail with "anthropic-workspace-id is required", set this.
# Find it at https://console.anthropic.com/settings/workspaces
# ANTHROPIC_WORKSPACE_ID=wrkspc_...
