# Your Anthropic API key — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Only identity-linked keys need this; workspace-scoped keys don't.
# If a request fails with "not scoped to a workspace", set it here.
ANTHROPIC_WORKSPACE_ID=
