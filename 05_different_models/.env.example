# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Only needed if your key is identity-linked rather than scoped to a workspace.
# Without it such keys fail with "must include the anthropic-workspace-id header".
# ANTHROPIC_WORKSPACE_ID=wrkspc_...
